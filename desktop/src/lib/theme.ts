// Copyright (C) 2025-2026 Noldarim
// SPDX-License-Identifier: AGPL-3.0-or-later

/**
 * Runtime theme constants for values that must be passed as JSX props
 * (e.g. component `color` attributes that don't accept CSS custom properties).
 *
 * All values here MUST stay in sync with _tokens.css.
 */
export const theme = {
  canvasDotColor: "rgba(62, 198, 224, 0.12)",
} as const;
