# Project Memory

- Rebuild and restart the AI agent/worker whenever code it depends on changes (for example: workflow logic, observability/event parsing, or step-context propagation), otherwise runtime behavior may stay stale.
